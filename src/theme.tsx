import { createTheme } from "@mui/material";

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#5c6bc0",
    },
    secondary: {
      main: "#00e676",
    },
  },
});

export default darkTheme;
